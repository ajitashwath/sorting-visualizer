<section class="py-16 md:py-24 px-4">
  <div class="container mx-auto text-center">
    <h1 class="text-4xl md:text-6xl font-bold mb-6">
      <span class="text-white">Sorting</span>
      <span class="text-primary-400">Visualizer</span>
    </h1>
    
    <p class="text-xl md:text-2xl text-white/70 max-w-3xl mx-auto mb-10">
      Visualize and understand sorting algorithms through interactive animations and detailed explanations
    </p>
    
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <a href="#visualizer" class="btn btn-primary">Start Visualizing</a>
      <a href="#algorithms" class="btn btn-outline">Learn Algorithms</a>
    </div>
    
    <div class="mt-16 relative">
      <div class="absolute -inset-0.5 bg-gradient-to-r from-primary-600/20 to-primary-400/20 rounded-lg blur opacity-75"></div>
      <div class="relative bg-dark-700 p-6 rounded-lg">
        <div class="flex justify-center items-end h-32 gap-1">
          {Array.from({ length: 20 }).map((_, i) => (
            <div 
              class="w-5 bg-primary-500 rounded-t-sm" 
              style={`height: ${Math.floor(Math.random() * 100) + 20}px; opacity: ${0.5 + (i / 40)}`}>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>